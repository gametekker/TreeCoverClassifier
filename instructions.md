# Steps

- `python3 -m venv venv`
- `source venv/bin/activate`
- `pip install -r requirements.txt`

- download the data from https://archive.ics.uci.edu/dataset/31/covertype
- extract and copy `covtype.data.gz` to the top level directory
